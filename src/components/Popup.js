import React from 'react';

function Popup (data) {
    return (
        <div className="overlay" onClick={() => console.log("Remove popup")}>

        

        </div>
    );
}

export default Popup;

